---
description: TypeScript interfaces and naming conventions
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# Naming

- `[Name]Props` — component props
- `[Name]Model` — domain entity / data fragment
- `[Name]ApiModel` — full API request/response

Place types in folder `types.ts`, or `shared/types` when reused.
